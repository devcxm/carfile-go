## Changes

- Uses the same pure Go decoding path on Darwin, Linux, and Windows.
- Removes cgo and platform-specific runtime decoder dependencies.
- Keeps support for default, lossless, palette, and chunked image encodings.

## Verification

- Tested complete resource extraction with representative Asset Catalog files.
- Verified all release targets build with `CGO_ENABLED=0`.
